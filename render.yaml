services:
  - type: web
    name: maps-scraper
    env: docker
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: scraper-db
          property: connectionString

databases:
  - name: scraper-db
    plan: free
